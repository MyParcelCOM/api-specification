{
  "get": {
    "tags": [
      "Services"
    ],
    "summary": "Get all the services.",
    "description": "This endpoint retrieves services.",
    "parameters": [
      {
        "in": "query",
        "name": "filter[from][country_code]",
        "type": "string",
        "description": "The country code from which the shipment is sent."
      },
      {
        "in": "query",
        "name": "filter[from][region_code]",
        "type": "string",
        "description": "The region code from which the shipment is sent."
      },
      {
        "in": "query",
        "name": "filter[to][country_code]",
        "type": "string",
        "description": "The country code to which the shipment is sent."
      },
      {
        "in": "query",
        "name": "filter[to][region_code]",
        "type": "string",
        "description": "The region code to which the shipment is sent."
      }
    ],
    "responses": {
      "200": {
        "description": "Retrieved the service(s).",
        "schema": {
          "type": "object",
          "required": [
            "data",
            "meta",
            "links"
          ],
          "additionalProperties": false,
          "properties": {
            "data": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Service"
              }
            },
            "meta": {
              "type": "object",
              "required": [
                "total_pages"
              ],
              "properties": {
                "total_pages": {
                  "type": "number",
                  "example": 13
                }
              }
            },
            "links": {
              "type": "object",
              "required": [
                "self",
                "first",
                "last"
              ],
              "properties": {
                "self": {
                  "type": "string",
                  "example": "$base_url/services?page[number]=3&page[size]=30"
                },
                "first": {
                  "type": "string",
                  "example": "$base_url/services?page[number]=1&page[size]=30"
                },
                "prev": {
                  "type": "string",
                  "example": "$base_url/services?page[number]=2&page[size]=30"
                },
                "next": {
                  "type": "string",
                  "example": "$base_url/services?page[number]=4&page[size]=30"
                },
                "last": {
                  "type": "string",
                  "example": "$base_url/services?page[number]=13&page[size]=30"
                }
              }
            }
          }
        }
      }
    }
  }
}

{
  "get": {
    "tags": [
      "Shops"
    ],
    "summary": "🚧 Retrieve shops by id.",
    "description": "This endpoint retrieves the shops with supplied id. A comma separated list of ids can be supplied to retrieve multiple shops.",
    "parameters": [
      {
        "in": "path",
        "name": "shop_id",
        "type": "string",
        "required": true,
        "description": "Id or comma separated list of id's of the shop(s)."
      }
    ],
    "responses": {
      "200": {
        "description": "Retrieved the shop(s).",
        "schema": {
          "type": "object",
          "properties": {
            "data": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Shop"
              }
            }
          }
        }
      }
    }
  }
}

{
  "get": {
    "tags": [
      "Webhooks"
    ],
    "summary": "Get a webhook by id.",
    "description": "This endpoint retrieves the webhook with the supplied id.",
    "parameters": [
      {
        "in": "path",
        "name": "webhook_id",
        "type": "string",
        "required": true,
        "description": "Id of the webhook."
      }
    ],
    "responses": {
      "200": {
        "description": "Retrieved the webhook.",
        "schema": {
          "type": "object",
          "properties": {
            "data": {
              "$ref": "#/definitions/Webhook"
            }
          }
        }
      }
    }
  }
}

{
  "get": {
    "parameters": [
      {
        "in": "query",
        "name": "filters[country_code]",
        "required": true,
        "type": "string",
        "description": "The from country code."
      },
      {
        "in": "query",
        "name": "filters[region_code]",
        "type": "string",
        "description": "The region code."
      },
      {
        "in": "query",
        "name": "filters[carrier_id]",
        "type": "string",
        "description": "The carrier id."
      },
    ],
    "tags": [
      "Locations"
    ],
    "summary": "Get all the locations, grouped by carrier.",
    "description": "Get all the locations, grouped by carrier.",
    "responses": {
      "200": {
        "description": "ALL the locations.",
        "schema": {
          "type": "object",
          "properties": {
            "data": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Location"
              }
            },
            "meta": {
              "type": "object",
              "properties": {
                "errors": {
                  "type": "object",
                  "properties": {
                    "code": {
                      "type": "string",
                      "example": "1234"
                    },
                    "title": {
                      "type": "string",
                      "example": "cannot retrieve carrier locations"
                    },
                    "detail": {
                      "type": "string",
                      "example": "An error occured while retrieving locations."
                    },
                    "meta": {
                      "type": "object",
                      "example": {
                        "locations": {
                          "links": {
                            "self": "/carriers/aaaaaa-bbbb-cccc-dddd-eeeeeeeeeeee/locations"
                          }
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}

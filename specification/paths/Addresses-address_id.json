{
  "get": {
    "tags": [
      "Addresses"
    ],
    "summary": "Get an address by id.",
    "description": "This endpoint retrieves the address with the supplied id.",
    "parameters": [
      {
        "in": "path",
        "name": "address_id",
        "type": "string",
        "required": true,
        "description": "Id of the address."
      }
    ],
    "responses": {
      "200": {
        "description": "Retrieved the address.",
        "schema": {
          "type": "object",
          "properties": {
            "data": {
              "$ref": "#/definitions/Address"
            }
          }
        }
      }
    }
  },
  "delete": {
    "tags": [
      "Addresses"
    ],
    "summary": "Delete an address by id.",
    "description": "This endpoint deletes the address with the supplied id.",
    "parameters": [
      {
        "in": "path",
        "name": "address_id",
        "type": "string",
        "required": true,
        "description": "Id of the address."
      }
    ],
    "responses": {
      "202": {
        "description": "The address will be deleted."
      },
      "204": {
        "description": "The address is deleted."
      }
    }
  }
}

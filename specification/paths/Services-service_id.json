{
  "get": {
    "tags": [
      "Services"
    ],
    "summary": "Retrieve services by id.",
    "description": "This endpoint retrieves the service with supplied id. A comma separated list of ids can be supplied to retrieve multiple services.",
    "parameters": [
      {
        "in": "path",
        "name": "service_id",
        "type": "string",
        "required": true,
        "description": "Id or comma separated list of id's of the service(s)."
      }
    ],
    "responses": {
      "200": {
        "description": "Retrieved the service(s).",
        "schema": {
          "type": "object",
          "properties": {
            "data": {
              "type": "array",
              "items": {
                "$ref": "#/definitions/Service"
              }
            }
          }
        }
      }
    }
  }
}

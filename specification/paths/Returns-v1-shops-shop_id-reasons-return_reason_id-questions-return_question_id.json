{
  "parameters": [
    {
      "$ref": "#/components/parameters/path-shop_id"
    },
    {
      "$ref": "#/components/parameters/path-return_reason_id"
    },
    {
      "$ref": "#/components/parameters/path-return_question_id"
    }
  ],
  "delete": {
    "tags": [
      "ReturnQuestions"
    ],
    "security": [
      {
        "OAuth2": [
          "returns.manage"
        ]
      }
    ],
    "summary": "Delete return question from shop return reason.",
    "description": "This endpoint deletes a return question from the shop return reason.",
    "responses": {
      "204": {
        "description": "The return question is deleted from the shop return reason."
      },
      "404": {
        "description": "No return question was found with the given return question and shop return reason."
      }
    }
  }
}

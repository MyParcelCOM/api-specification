{
  "type": "object",
  "required": [
    "id",
    "type",
    "attributes"
  ],
  "properties": {
    "id": {
      "$ref": "../definitions/Uuid.json"
    },
    "type": {
      "type": "string",
      "pattern": "^system-messages$",
      "example": "system-messages"
    },
    "attributes": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "example": "alert"
        },
        "filter": {
          "type": "string",
          "example": "shipment"
        },
        "content": {
          "type": "string",
          "example": "Carrier X is currently not available"
        }
      }
    }
  }
}

{
  "type": "object",
  "required": [
    "resource_type",
    "resource_action",
    "predicate",
    "action"
  ],
  "properties": {
    "resource_type": {
      "type": "string",
      "example": "services"
    },
    "resource_action": {
      "type": "string",
      "enum": [
        "create",
        "read",
        "update",
        "delete"
      ],
      "example": "create"
    },
    "predicate": {
      "$ref": "#/components/schemas/Predicate"
    },
    "action": {
      "$ref": "#/components/schemas/Action"
    }
  }
}

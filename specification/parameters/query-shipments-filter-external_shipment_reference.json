{
  "name": "filter[external_shipment_reference]",
  "in": "query",
  "description": "Search for a shipment by its external shipment reference attribute. This is a strict search, meaning that only if the search string matches the `external_shipment_reference` attribute of a shipment completely, will the shipment be returned.",
  "schema": {
    "type": "string"
  }
}
